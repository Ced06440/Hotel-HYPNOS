{% extends 'base.html.twig' %}

{% block title %}Réservation chambre Hypnos Chamonix{% endblock %}

{% block content %}

{% for hotel in hotelChamonix %}

<div class="carousel-inner">
    <div class="carousel-item active">
        <img class="hotel" src="/assets/{{hotel.photoBandeau}}" alt="">
        <div class="container">
            <div class="carousel-caption">
                <div class='hotel-logo-name'>
                    <img class='logo' src="{{asset('assets/img/Logo_HYPNOS.png')}}" alt="">
                </div>
            </div>
        </div>
    </div>
</div>

{% endfor %}

<div class="container">
    <div class="text-center"><h1>Réservation HYPNOS CHAMONIX</h1></div>

{% for message in app.flashes('warning') %}
    <p class="text-center" style="color:red;">{{ message }}</p>
{% endfor %}

    {{form_start(bookingChamonixForm)}}

    <div class="row justify-content-center align-items-center text-center">
            {{ form_label(bookingChamonixForm.startDate)}}
            <input class="form-control" {{form_widget(bookingChamonixForm.startDate)}}
    </div>

    <div class="row justify-content-center align-items-center text-center">
        <div class="text-bold">{{ form_label(bookingChamonixForm.endDate)}}</div>
        <input class="form-control"{{form_widget(bookingChamonixForm.endDate)}}
    </div>

    <div class="row justify-content-center align-items-center text-center">
        {{ form_label(bookingChamonixForm.rooms)}}
        <div style='width:320px'>{{form_widget(bookingChamonixForm.rooms)}}</div>
    </div>

    <div class="text-center mt-4">
        {{ form_row(bookingChamonixForm.submit) }}
    </div>

    {{form_end(bookingChamonixForm)}}
</div>

{% endblock %}
